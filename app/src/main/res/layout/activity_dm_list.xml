<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/dm_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/heartbeat_animated_bg">

    <!-- Background Animation Container -->
    <FrameLayout
        android:id="@+id/love_animation_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0.3"
        android:clickable="false"
        android:focusable="false" />

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/dm_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:title="Direct Messages"
        app:titleTextColor="@color/white"
        app:contentInsetStartWithNavigation="0dp"
        app:layout_constraintTop_toTopOf="parent">

        <RelativeLayout
            android:id="@+id/btn_requests"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="end"
            android:layout_marginEnd="8dp"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_centerInParent="true"
                android:src="@android:drawable/ic_menu_myplaces"
                app:tint="@color/white" />

            <TextView
                android:id="@+id/requests_badge"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_alignParentTop="true"
                android:layout_alignParentEnd="true"
                android:layout_marginTop="4dp"
                android:layout_marginEnd="4dp"
                android:background="@drawable/circle_pink"
                android:gravity="center"
                android:text="0"
                android:textColor="@color/white"
                android:textSize="10sp"
                android:textStyle="bold"
                android:visibility="gone" />
        </RelativeLayout>
    </com.google.android.material.appbar.MaterialToolbar>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/dm_list_recycler"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:padding="8dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toBottomOf="@id/dm_toolbar"
        app:layout_constraintBottom_toTopOf="@id/bottom_nav_container"
        tools:listitem="@layout/item_chat_compact" />

    <TextView
        android:id="@+id/no_dms_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="No messages yet"
        android:textColor="@color/melodify_text_secondary"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/dm_toolbar"
        app:layout_constraintBottom_toTopOf="@id/bottom_nav_container"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Bottom Nav & Mini Player container -->
    <LinearLayout
        android:id="@+id/bottom_nav_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- Navigation Bar -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <LinearLayout style="@style/NavItem" android:id="@+id/nav_lobby" android:clickable="true" android:focusable="true" android:background="?attr/selectableItemBackground">
                <ImageView style="@style/NavIcon" android:src="@android:drawable/ic_menu_today" app:tint="@color/melodify_text_secondary" />
                <TextView style="@style/NavText" android:text="Lobby" android:textColor="@color/melodify_text_secondary" />
            </LinearLayout>

            <LinearLayout style="@style/NavItem" android:id="@+id/nav_search" android:clickable="true" android:focusable="true" android:background="?attr/selectableItemBackground">
                <ImageView style="@style/NavIcon" android:src="@android:drawable/ic_menu_search" app:tint="@color/melodify_text_secondary" />
                <TextView style="@style/NavText" android:text="Search" android:textColor="@color/melodify_text_secondary" />
            </LinearLayout>

            <LinearLayout style="@style/NavItem" android:id="@+id/nav_dm" android:clickable="true" android:focusable="true" android:background="?attr/selectableItemBackground">
                <ImageView style="@style/NavIcon" android:src="@android:drawable/stat_notify_chat" app:tint="@color/melodify_pink" />
                <TextView style="@style/NavText" android:text="DM" android:textColor="@color/melodify_pink" />
            </LinearLayout>

            <LinearLayout style="@style/NavItem" android:id="@+id/nav_profile" android:clickable="true" android:focusable="true" android:background="?attr/selectableItemBackground">
                <ImageView style="@style/NavIcon" android:src="@android:drawable/ic_menu_myplaces" app:tint="@color/melodify_text_secondary" />
                <TextView style="@style/NavText" android:text="Profile" android:textColor="@color/melodify_text_secondary" />
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>